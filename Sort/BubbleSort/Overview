버블정렬
- 현재 위치와 다음 위치의 값을 비교해 정렬 하는 방식

예) 1 6 7 3 4 5 8 10 9 2
처음 주기에서 [0]와 [1]을 비교해 정렬하고 [1]과 [2]를 비교해
정렬하고 [2]와 [3]을 비교해 정렬 해 나간다.
다음 주기에서 동일하게 [1]과 [2]를 비교해 정렬해나간다.

시간복잡도
총 비교 횟수 10 + 9 + 8 + ... + 3 + 2 + 1
일반식 = N * (N+1) / 2

단점
버블정렬은 선택정렬보다 느리다. 그 이유는 
버블정렬은 매 2개의 원소간 swap을 시행하는 반면
선택정렬은 최소값을 찾은뒤 한번의 swap을 시행하기 때문이다.
